# Only needed when building for GitHub Pages project sites, where the app lives under /<repo-name>/.
# The deploy workflow sets this automatically; leave it unset for local development.
# NEXT_PUBLIC_BASE_PATH=/paintmatcher
